#include <stdio.h>

// 과거에는 이렇게 했지만, 최근에는 하지 않음
// #define TRUE 1
// #define FALSE 0

int main()
{
    /* int tv, fv;
    tv = (1 < 2);   // true
    fv = (1 > 2);   // false

    printf("True is %d\n", tv); // 1
    printf("False is %d\n", fv); // 0 */ 

    /* int i = -5;
    while(i)
        printf("%d is true\n", i++);
    printf("%d is false\n", i);
    // 0(false)외의 값은 true */

    while(1)    // 무한루프, 서버 돌리는 등(계속 작동해야하는 경우)
    {
        // 항상 true로 하고 싶은 경우 
        // 프로그램 종료원하는 경우 while문 안에서 특정조건 만족하면 while문을 깨고 나오도록
    }
}