/*
 * Written by J.-M. Hong
 */ 

// 주석을 달아야 할 곳에는 달고, 달지 말아야 할 곳에는 달지 말기!

#include <stdio.h>

int main()
{
    /*
        This is my code
    */

    // variables (x)
    int x, y, sum;

    x = 1;
    y = 4;

    sum = x + y; // add two numbers (x) --> 변수명을 sum으로 바꾸어서 사용

    printf("\n%i + %i = %i", x, y, sum);
    printf("\a");

    return 0;
}