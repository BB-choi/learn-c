int main()
{
    // int x; // declaration
    // int y;
    // int z;

    // int x, y, z;

    // x = 1; // assignment
    // y = 2;

    // int x = 1, y= 2, z;
    // int x = 1, y= 2, z = x + y; 
    // 초기화와 연산이 함께 있기 때문에 쪼개는 것이 좋다.

    int x = 1, y = 2;
    int z;

    z = x + y;

    return 0;
}